import { getToflitFlowsByCsv, getToflitFlowsByApi } from '../../helpers/misc';
import GetToflitFlowsComponent from '../../components/GetToflitFlowsComponent';
import VegaWithMiscFunctions from '../../components/VegaWithMiscFunctions';
import KeplerWrapper from '../../components/KeplerWrapper';

export const Box = () => (
  <div style={{padding: 30, backgroundColor: 'tomato'}} />
)

<KeplerWrapper/>

# Tu croyais en apprendre plus sur la manière dont le document de synthèse a été produit et comment tu peux l'exploiter ?
_Désolé_ ici tu ne pourras que voir mes piètres tests avec MDX.

Déjà je sais utiliser du **markdown** avec <span style={{color: "blue"}}>JSX</span> : il parait que ça s'appelle du **MDX** !

En tout cas j'ai visionné une [vidéo](https://www.youtube.com/watch?v=d2sQiI5NFAM) très sympathique à ce sujet cet aprèm :)

<Box> Et je tente des choses avec mon composant Box </Box>

Tu peux aussi voir ici mes tests sur les fonctions de récupération et filtrage des données
=> à terme ce seraient probablement des components insérés dans le mdx

## Test component pour fonctions misc -- getToflitFlowsByCsv

<GetToflitFlowsComponent chosen_function = {getToflitFlowsByCsv}/>

<GetToflitFlowsComponent chosen_function = {getToflitFlowsByApi}/>

## Test component vega pour histogramme se nourrisant de getToflitFlowsByCsv

<VegaWithMiscFunctions
      chosen_function={getToflitFlowsByCsv}
      spec={{
        "description": "Flows selon les bureaux de Fermes",
        "mark": "bar",
        "encoding": {
          "x": {
            "field": "customs_office",
              "type": "nominal"
          },
          "y": {
            "field": "value",
            "aggregate": "mean", 
            "type": "quantitative"
          }
        }
      }}
    />